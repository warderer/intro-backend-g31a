1. `npm init` Contestar preguntas
2. `npm i express` Instalar paquete express
3. Agregar propiedad `"type": "module"` al archivo `package.json`
4. Agregar script `"start": "node app.js"` al archivo `package.json`
